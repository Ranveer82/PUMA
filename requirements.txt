numpy>=1.21
pandas>=1.3
matplotlib>=3.4
scipy>=1.7
pyemu>=1.1

# Optional (only for spatial maps / Marthe field rendering):
#   geopandas        - shapefile overlays on spatial maps
#   pymarthe (github)- render adjusted property fields on the model grid
#     pip install git+https://github.com/pymartheproject/pymarthe.git
